<nav class="navbar navbar-default">
  <%= link_to root_path, class: 'app-name' do %>
    <h1>Churches Help</h1>
  <% end %>

  <ul class="nav navbar-nav header-user-controls">
    <% if user_signed_in? %>
      <li>
        <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: 'btn' %>
      </li>
      <li>
        <%= link_to 'Settings',edit_user_registration_path, class: 'btn fa-before fa-gear' %>
      </li>
    <% else %>
      <li id="sign_in">
        <a href="#sign_in_dialog" role="button" class="btn sign-in-button" data-return-to="<%= request.fullpath %>">Sign In</a>
      </li>
      <li>
        <%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-primary' %>
      </li>
    <% end %>
  </ul>
</nav>
