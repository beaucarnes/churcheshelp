<%= content_for(:header_text, 'Listing Churches') %>

<%= render 'shared/locations_table', locations: @locations %>

<%= render 'shared/actions', links: [
  user_signed_in? ? ['New Church', new_location_path] : nil,
  ['Regions', regions_path],
  ['Events', events_path]
] %>
