<%= content_for(:header_text, "Editing Church") %>

<% if @location.archived? %>
  <div class="alert alert-warning">
    This church is archived, and can no longer be used for events.
  </div>
<% end %>

<%= render 'form' %>

<%= render 'shared/actions', links: [
  ['Show', @location],
  ['Back', locations_path]
] %>

